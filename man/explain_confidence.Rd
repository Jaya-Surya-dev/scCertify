% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explain_confidence.R
\name{explain_confidence}
\alias{explain_confidence}
\title{Explain Confidence Attribution}
\usage{
explain_confidence(object, cell_id)
}
\arguments{
\item{object}{A Seurat or SingleCellExperiment object.}

\item{cell_id}{Cell barcode.}
}
\value{
Character vector describing the factors contributing to the
confidence score.
}
\description{
Explains why a cell received
its confidence score.
}
\examples{
if (requireNamespace("Seurat", quietly = TRUE)) {
  counts <- matrix(
    rpois(200, lambda = 5),
    nrow = 20
  )

  rownames(counts) <- paste0("Gene", seq_len(20))
  colnames(counts) <- paste0("Cell", seq_len(10))

  obj <- Seurat::CreateSeuratObject(
    counts = counts
  )

  obj$predicted_label <- rep(
    c("T cell", "B cell"),
    each = 5
  )

  obj$marker_score <- runif(ncol(obj))
  obj$neighbor_score <- runif(ncol(obj))
  obj$entropy_norm <- runif(ncol(obj))
  obj$doublet_score <- runif(ncol(obj))

  explain_confidence(
    obj,
    cell_id = "Cell1"
  )
}

}
