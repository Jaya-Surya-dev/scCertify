% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explain_cell.R
\name{explain_cell}
\alias{explain_cell}
\title{Explain Cell Annotation Confidence}
\usage{
explain_cell(object, cell_id)
}
\arguments{
\item{object}{A Seurat or SingleCellExperiment object.}

\item{cell_id}{Cell barcode or column name.}
}
\value{
A character vector containing the interpretation.
}
\description{
Provides an interpretable explanation for the confidence score
assigned to an individual cell.
}
\examples{
if (requireNamespace("Seurat", quietly = TRUE)) {
  counts <- matrix(
    rpois(200, lambda = 5),
    nrow = 20
  )

  rownames(counts) <- paste0("Gene", seq_len(20))
  colnames(counts) <- paste0("Cell", seq_len(10))

  obj <- Seurat::CreateSeuratObject(
    counts = counts
  )

  obj$predicted_label <- rep(
    c("T cell", "B cell"),
    each = 5
  )

  obj$marker_score <- runif(ncol(obj))
  obj$neighbor_score <- runif(ncol(obj))
  obj$entropy_norm <- runif(ncol(obj))
  obj$confidence_score <- runif(ncol(obj))

  explain_cell(
    obj,
    cell_id = "Cell1"
  )
}

}
